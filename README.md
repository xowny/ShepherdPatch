# ShepherdPatch Source Code Only

source code of ShepherdPatch.

Included:
- native patch source in `src/`
- native tests in `tests/`
- WinUI configurator source in `gui/`
- root build/config files: `CMakeLists.txt`, `ShepherdPatch.ini`

Excluded:
- build outputs
- published binaries
- local scratch/research folders
- game binaries and install artifacts
- temporary GUI artifacts (`bin/`, `obj/`)

Suggested next steps:
1. `git init`
2. `git add .`
3. `git commit -m "Initial source import"`
